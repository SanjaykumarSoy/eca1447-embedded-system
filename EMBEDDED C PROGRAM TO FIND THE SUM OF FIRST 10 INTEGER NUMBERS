#include <reg51.h>

void main() {
    unsigned char sum = 0; 
    unsigned char i;

    for (i = 1; i <= 10; i++) {
        sum += i; // This is equivalent to sum = sum + i;
    }

    ACC = sum; // Store the sum in the accumulator
    P0 = sum;  // Output the sum to port 0
}
